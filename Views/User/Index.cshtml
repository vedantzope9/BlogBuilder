@{
    ViewData["Title"] = "Welcome to BlogBuilder";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>BlogBuilder | Create. Manage. Explore.</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&display=swap" rel="stylesheet" />

    <style>
        :root {
            --bg-light: linear-gradient(to right, #fdfbfb, #ebedee, #d9f0ff);
            --bg-dark: linear-gradient(to right, #1a1a1a, #111111);
            --text-light: #1a1a1a;
            --text-dark: #f2f2f2;
            --primary: #8e44ad;
            --accent: #ffbe0b;
            --feature-bg-light: #ffffff;
            --feature-bg-dark: #2b2b2b;
            --footer-dark: #121212;
            --btn-glow: 0 0 12px rgba(138, 43, 226, 0.5);
        }

        body {
            font-family: 'DM Sans', sans-serif;
            margin: 0;
            background: var(--bg-light);
            color: var(--text-light);
            transition: background 0.5s ease, color 0.5s ease;
            background-repeat: no-repeat;
            background-size: cover;
        }

        body.dark-mode {
            background: var(--bg-dark) !important;
            color: var(--text-dark);
        }


        .hero {
            padding: 8rem 1rem 6rem;
            text-align: center;
            animation: fadeSlide 1s ease-out;
        }

        .hero h1 {
            font-size: 3.8rem;
            font-weight: 800;
            margin-bottom: 1.2rem;
            background: linear-gradient(to right, #9b59b6, #e67e22);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

       .hero p {
            font-size: 1.35rem;
            max-width: 750px;
            margin: 0 auto 2.2rem;
            font-weight: 400;
            line-height: 1.8;
            color: inherit;
            transition: color 0.4s ease;
       }

       .cta-btn {
            padding: 0.8rem 2.2rem;
            border: none;
            border-radius: 50px;
            background: linear-gradient(120deg, var(--primary), #5e2a7a);
            background-size: 200%;
            background-position: left;
            color: #fff;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.4s ease;
            box-shadow: var(--btn-glow);
            position: relative;
            overflow: hidden;
       }

        .cta-btn:hover {
            background-position: right;
            transform: scale(1.05);
            box-shadow: 0 0 18px rgba(138, 43, 226, 0.6);
        }


        .cta-btn:active {
            animation: clickScale 0.2s ease;
        }

        @@keyframes clickScale {
            0%{
                transform: scale(1.05);
            }

            50% {
                transform: scale(0.95);
            }

            100% {
                transform: scale(1);
            }
        }

        .features {
            padding: 5rem 1rem;
            background-color: var(--feature-bg-light);
            transition: background-color 0.3s;
        }

        body.dark-mode .features {
            background-color: var(--feature-bg-dark);
        }

        .feature-box {
            padding: 2rem;
            border-radius: 12px;
            background-color: #f8f9fc;
            transition: background-color 0.3s, transform 0.3s;
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.05);
            border: 2px solid #444; /* Dark border */
        }

        body.dark-mode .feature-box {
            background-color: #373737;
            border-color: #666; /* Softer dark border */
        }

        .feature-box:hover {
            transform: translateY(-6px);
        }

        .feature-box i {
            font-size: 2.5rem;
            color: var(--primary);
            margin-bottom: 1rem;
        }

        .feature-box h4 {
            font-size: 1.5rem;
            font-weight: 600;
        }

        .feature-box p {
            font-size: 1.05rem;
            font-weight: 400;
            opacity: 0.95;
        }

        @@keyframes fadeSlide {
            0%{
                opacity: 0;
                transform: translateY(30px);
            }

            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @@media (max-width: 768px) {
            .hero h1{
                font-size: 2.4rem;
            }

            .hero p {
                font-size: 1.1rem;
            }
        }

        @@keyframes slideBg {
            0%{
                background-position: left;
            }

            50% {
                background-position: right;
            }

            100% {
                background-position: left;
            }

        }
        .cta-btn.clicked {
            animation: slideBg 1s ease-in-out;
        }

    </style>
</head>
<body>
    <section class="hero">
        <h1>Welcome to BlogBuilder</h1>
        <p>
            <span style="font-weight: 500;">Unleash your creativity</span> and give life to your thoughts through <strong>stunning, expressive blogs</strong>.<br />
            Whether you’re sharing stories, documenting your journey, or writing in-depth tech guides — <span style="color: var(--primary); font-weight: 600;">BlogBuilder</span> is your creative companion, helping your voice find the space it deserves.
        </p>

        <a href= "LoginUser" class="cta-btn">Get Started</a>
    </section>

    <!-- Features -->
    <section class="features text-center">
        <div class="container">
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="feature-box h-100">
                        <i class="fas fa-pen-nib"></i>
                        <h4>Modern Blog Editor</h4>
                        <p>Create beautiful content with our rich editor that supports markdown, autosave, and easy formatting.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="feature-box h-100">
                        <i class="fas fa-search"></i>
                        <h4>Discover & Search</h4>
                        <p>Explore blogs by topic, keyword, or latest trends — and never run out of content to read or get inspired from.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="feature-box h-100">
                        <i class="fas fa-book-open"></i>
                        <h4>Your Digital Journal</h4>
                        <p>Build your personal library. Reflect. Revisit. Organize your thoughts and drafts — all in one place.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

</body>
</html>
